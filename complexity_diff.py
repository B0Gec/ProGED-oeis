"""
To compare complexity of outputed equations by Diofantos vs. ground truth

id ... gt cx .. Diof cx
dist(freq gt cx(i) / cx i )
dist(freq Diof cx(i) / cx i )
cx = order vs. nonzero.

# ( Diof cx (id) < gt cx (id) )  |  # ( Diof cx = gt cx)  |  # ( Diof cx > gt cx)
# ( Diof cx (id) < gt cx (id) )  |  # ( Diof cx = gt cx)  |  # ( Diof cx > gt cx)



"""